/* 
 * As for classes, the name of public interface should be equal to the name of file, which contains it.
 * The file should contain only 1 or 0 public interfaces.
 */
public interface InterfaceDeclaration {}

/* 
 * Interface is an abstract entity, so the 'abstract' word could be placed without any compilation errors.
 * But there's no need for it, keep it simple :)
 */
abstract interface InterfaceWithExplicitAbstractModifier {}